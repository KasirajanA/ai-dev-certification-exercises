// URL Shortener Tests
//
// TODO: Ask Claude Code to write comprehensive tests here FIRST.
//
// Then run: npm test
// All tests should FAIL (RED phase).
//
// After writing the implementation, run again:
// All tests should PASS (GREEN phase).

import { describe, it, expect } from "vitest";

describe("URL Shortener", () => {
  it("should have tests written by Claude Code", () => {
    // Ask Claude: "Write comprehensive tests for the URL shortener.
    // Cover: valid URLs, invalid URLs, short code format,
    // idempotency, retrieval, and edge cases.
    // Do NOT implement the shortener — tests only."
    expect(true).toBe(false); // Remove this — it's a placeholder
  });
});
